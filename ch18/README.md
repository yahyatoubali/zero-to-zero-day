# Chapter 18: Integer Overflows, Format Strings, Races

## Listings
- `vuln.c` — Integer overflow to heap overflow
- `fmt.c` — Format string vulnerability (leak + write via %n)
- `challenge.c` — Integer underflow in packet parsing
- `exploit_fmt.py` — Format string GOT overwrite exploit

## Labs
1. Guided Lab 1: Integer overflow to heap overflow
2. Guided Lab 2: Format string leak and write (GOT overwrite)
3. Unguided Challenge: Integer underflow exploit
